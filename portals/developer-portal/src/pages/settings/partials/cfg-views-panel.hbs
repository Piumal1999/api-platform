      <!-- ══ Manage Views ══ -->
      <div class="dp-settings__panel" id="dp-settings__views">
        <div class="dp-settings__panel-header">
          <div>
            <h2 class="dp-settings__panel-title">Views</h2>
            <p class="dp-settings__panel-desc">Views scope which labelled APIs are visible. LLM Instructions and API Workflows are configured per view.</p>
          </div>
          <button class="dp-settings__btn-primary" id="dp-settings__add-view-btn" type="button">
            <i class="bi bi-plus"></i> Add view
          </button>
        </div>

        <div class="dp-settings__table-wrap">
          <table class="dp-settings__table">
            <thead>
              <tr>
                <th>Handle</th>
                <th>Name</th>
                <th>Labels</th>
                <th style="width:90px;"></th>
              </tr>
            </thead>
            <tbody id="dp-settings__views-tbody">
              {{#views}}
              <tr id="dp-settings__view-row-{{id}}" data-id="{{id}}">
                <td><span class="dp-settings__name-chip">{{id}}</span></td>
                <td>
                  <button type="button" class="dp-settings__view-edit-btn" data-id="{{id}}"
                    style="border:none;background:none;padding:0;cursor:pointer;font-size:.875rem;font-weight:600;color:#1a2433;text-decoration:none;">{{displayName}}</button>
                </td>
                <td>
                  <div class="d-flex flex-wrap gap-1">
                    {{#each labels}}<span class="dp-settings__name-chip">{{this}}</span>{{/each}}
                  </div>
                </td>
                <td>
                  <div class="dp-settings__row-actions">
                    <button class="dp-settings__icon-btn dp-settings__view-edit-btn" type="button" data-id="{{id}}" title="Edit">
                      <i class="bi bi-pencil"></i>
                    </button>
                    {{#unless (eq id "default")}}
                    <button class="dp-settings__icon-btn dp-settings__icon-btn--danger dp-settings__view-delete-btn" type="button"
                      data-id="{{id}}" data-name="{{displayName}}" title="Delete">
                      <i class="bi bi-trash"></i>
                    </button>
                    {{/unless}}
                  </div>
                </td>
              </tr>
              {{/views}}
              {{#unless views.length}}
              <tr><td colspan="4" class="dp-settings__tbl-empty">No views yet. Add one to get started.</td></tr>
              {{/unless}}
            </tbody>
          </table>
        </div>
      </div><!-- /#dp-settings__views -->
