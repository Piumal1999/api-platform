<script type="application/json" id="apiWorkflowsDataContainer">
{{{ json apiWorkflows }}}
</script>

<script type="application/json" id="apiWorkflowsContextData">
{
    "orgId": "{{orgId}}",
    "viewName": "{{viewName}}",
    "csrfToken": "{{csrfToken}}"
}
</script>

<section id="apiWorkflowSection">

    {{!-- ── LIST VIEW ── --}}
    <div id="apiWorkflowList">
        <!-- Header -->
        <div class="dp-settings__panel-header">
            <div>
                <h2 class="dp-settings__panel-title">API Workflows</h2>
                <p class="dp-settings__panel-desc">Encode your real-world API use cases as guided workflows, so AI agents know exactly how your APIs work together, <br />
                    in the right order, for the right purpose. No guesswork, no hallucinations.</p>
            </div>
            {{#if apiWorkflows.length}}
            <button class="dp-settings__btn-primary" id="createApiWorkflowBtn" type="button">
                <i class="bi bi-plus me-1"></i> Create
            </button>
            {{/if}}
        </div>

        {{> view-combo hash="dp-settings__workflows" label="workflows"}}

        {{#if apiWorkflows.length}}
        <!-- Flows Grid -->
        <div class="dp-api__workflows-grid">
            {{#each apiWorkflows}}
            <div class="dp-api__workflow-card">
                <div class="dp-api__workflow-card-header">
                    <div class="dp-api__workflow-card-meta-row">
                        <span class="dp-api__workflow-status-dot dp-api__workflow-status-dot--{{lowercase this.status}}"></span>
                        <span class="dp-api__workflow-status-label">{{this.status}}</span>
                        <span class="dp-api__workflow-card-sep">·</span>
                        <span class="dp-api__workflow-content-type-badge dp-api__workflow-content-type--{{lowercase this.contentType}}">{{this.contentType}}</span>
                    </div>
                    <div class="dp-api__workflow-card-actions dp-api__workflow-card-actions--hover">
                        {{#unless (eq this.agentVisibility "HIDDEN")}}
                        <button type="button"
                            class="dp-api__workflow-action-btn dp-api__workflow-view-prompt-btn"
                            title="View agent prompt"
                            data-api-workflow-id="{{this.id}}">
                            <i class="bi bi-robot"></i>
                        </button>
                        {{/unless}}
                        <button type="button" class="dp-api__workflow-action-btn dp-api__workflow-edit-btn"
                            title="Edit"
                            data-api-workflow-id="{{this.id}}">
                            <i class="bi bi-pencil"></i>
                        </button>
                        <button type="button" class="dp-api__workflow-action-btn dp-api__workflow-action-btn--danger dp-api__workflow-delete-btn"
                            title="Delete"
                            data-api-workflow-id="{{this.id}}">
                            <i class="bi bi-trash"></i>
                        </button>
                    </div>
                </div>
                <div class="dp-api__workflow-card-body">
                    <h5 class="dp-api__workflow-card-title">{{this.displayName}}</h5>
                    <p class="dp-api__workflow-card-description" title="{{this.description}}">
                        {{this.description}}
                    </p>
                </div>
                <div class="dp-api__workflow-card-footer">
                    <div class="dp-api__workflow-card-visibility-badges">
                        {{#unless (eq this.agentVisibility "HIDDEN")}}
                        <span class="dp-api__workflow-visibility-badge dp-api__workflow-agent-visible"
                            title="AI agents can discover and use this workflow">
                            <span class="agent-live-dot"></span>
                            <i class="bi bi-robot"></i> AI Ready
                        </span>
                        {{/unless}}
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <div class="dp-empty">
            <span class="dp-empty-icon"><i class="bi bi-diagram-3"></i></span>
            <h2 class="dp-empty-title">No API Workflows yet</h2>
            <p class="dp-empty-desc">Create your first flow to define a multi-step API sequence.</p>
            <button type="button" class="dp-btn dp-btn--primary dp-btn--lg" id="createApiWorkflowBtnEmpty">
                <i class="bi bi-plus"></i> Create Your First Workflow
            </button>
        </div>
        {{/if}}
    </div>

    {{!-- ── CREATE / EDIT PANEL ── --}}
    <div id="apiWorkflowForm" style="display:none;">
        {{> create-api-workflow}}
    </div>

</section>

{{!-- ── DELETE CONFIRMATION MODAL ── --}}
<div class="modal fade" id="deleteApiWorkflowModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteApiWorkflowModalTitle"></h5>
            </div>
            <div class="modal-body">
                <p id="deleteApiWorkflowModalMessage"></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn dp-common__btn-link" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn dp-common__btn-primary dp-common__btn-danger" id="deleteApiWorkflowConfirmBtn" data-cyid="confirm">
                    Confirm
                </button>
            </div>
        </div>
    </div>
</div>

{{!-- ── AGENT PROMPT MODAL ── --}}
<div class="modal fade" id="agentPromptModal" tabindex="-1" aria-labelledby="agentPromptModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <div>
                    <h5 class="modal-title mb-0" id="agentPromptModalLabel">Agent Prompt</h5>
                    <small class="text-muted" id="agentPromptFlowName"></small>
                </div>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-0">
                <div class="dp-api__workflow-prompt-toolbar">
                    <span class="small text-muted">Use this prompt to let your AI agent execute the workflow—handling API calls, dependencies, and errors automatically</span>
                    <div class="dp-prompt__modal-toolbar">
                        <button type="button" class="dp-prompt__tool-btn" id="downloadPromptBtn" title="Download as .txt">
                            <i class="bi bi-download"></i>
                        </button>
                        <button type="button" class="dp-prompt__tool-btn" id="copyPromptBtn" title="Copy prompt">
                            <i class="bi bi-copy"></i>
                        </button>
                        <button type="button" class="dp-prompt__btn-run-prompt" id="runInClaudeBtn">
                            <i class="bi bi-play-fill"></i> Run in Claude
                        </button>
                    </div>
                </div>
                <pre id="agentPromptContent" class="dp-api__workflow-prompt-pre"></pre>
            </div>
        </div>
    </div>
</div>