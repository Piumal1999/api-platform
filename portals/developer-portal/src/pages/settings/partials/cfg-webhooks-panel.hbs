      <!-- ══ Webhooks ══ -->
      <div class="dp-settings__panel" id="dp-settings__webhooks">
        <div class="dp-settings__panel-header">
          <div>
            <h2 class="dp-settings__panel-title">Webhooks</h2>
            <p class="dp-settings__panel-desc">Send a notification to your own endpoint when API keys or subscriptions change in this organization.</p>
          </div>
          <button class="dp-settings__btn-primary" id="dp-settings__add-webhook-btn" type="button">
            <i class="bi bi-plus"></i> Add webhook
          </button>
        </div>

        <div class="dp-settings__table-wrap">
          <table class="dp-settings__table">
            <thead>
              <tr>
                <th>Display name</th>
                <th>Target URL</th>
                <th>Events</th>
                <th>Secret</th>
                <th>Public key</th>
                <th>Enabled</th>
                <th style="width:90px;"></th>
              </tr>
            </thead>
            <tbody id="dp-settings__webhooks-tbody">
              {{#webhookSubscribers}}
              <tr id="dp-settings__webhook-row-{{id}}" data-id="{{id}}">
                <td>
                  <button type="button" class="dp-settings__webhook-edit-btn" data-id="{{id}}"
                    style="border:none;background:none;padding:0;cursor:pointer;font-size:.875rem;font-weight:600;color:#1a2433;text-decoration:none;">{{displayName}}</button>
                </td>
                <td>
                  <span class="dp-settings__name-chip" title="{{targetUrl}}"
                    style="display:inline-block;max-width:200px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">{{targetUrl}}</span>
                </td>
                <td>
                  {{#if events.length}}
                  {{#each events}}<span class="dp-settings__name-chip" style="margin-right:4px;">{{this}}</span>{{/each}}
                  {{else}}
                  <span style="font-size:.813rem;color:#637282;">All events</span>
                  {{/if}}
                </td>
                <td>{{#if hasSecret}}••••••••{{else}}—{{/if}}</td>
                <td>{{#if hasPublicKey}}Configured{{else}}—{{/if}}</td>
                <td>{{#if enabled}}Yes{{else}}No{{/if}}</td>
                <td>
                  <div class="dp-settings__row-actions">
                    <button class="dp-settings__icon-btn dp-settings__webhook-edit-btn" type="button" data-id="{{id}}" title="Edit">
                      <i class="bi bi-pencil"></i>
                    </button>
                    <button class="dp-settings__icon-btn dp-settings__icon-btn--danger dp-settings__webhook-delete-btn" type="button"
                      data-id="{{id}}" data-name="{{displayName}}" title="Delete">
                      <i class="bi bi-trash"></i>
                    </button>
                  </div>
                </td>
              </tr>
              {{/webhookSubscribers}}
              {{#unless webhookSubscribers.length}}
              <tr><td colspan="7" class="dp-settings__tbl-empty">No webhooks yet. Add one to get started.</td></tr>
              {{/unless}}
            </tbody>
          </table>
        </div>
      </div><!-- /#dp-settings__webhooks -->
