      <!-- ══ Key Managers ══ -->
      <div class="dp-settings__panel" id="dp-settings__keymanagers">
        <div class="dp-settings__panel-header">
          <div>
            <h2 class="dp-settings__panel-title">Key Managers</h2>
            <p class="dp-settings__panel-desc">Register the OAuth2 key managers applications can obtain tokens from. OAuth applications are created directly in the key manager itself — the portal only proxies token requests to it.</p>
          </div>
          <button class="dp-settings__btn-primary" id="dp-settings__add-km-btn" type="button">
            <i class="bi bi-plus"></i> Add key manager
          </button>
        </div>

        <div class="dp-settings__table-wrap">
          <table class="dp-settings__table">
            <thead>
              <tr>
                <th>Display name</th>
                <th>Type</th>
                <th>Token endpoint</th>
                <th>Status</th>
                <th style="width:90px;"></th>
              </tr>
            </thead>
            <tbody id="dp-settings__keymanagers-tbody">
              {{#keyManagers}}
              <tr id="dp-settings__km-row-{{id}}" data-id="{{id}}">
                <td>
                  <button type="button" class="dp-settings__km-edit-btn" data-id="{{id}}"
                    style="border:none;background:none;padding:0;cursor:pointer;font-size:.875rem;font-weight:600;color:#1a2433;text-decoration:none;">{{displayName}}</button>
                </td>
                <td><span class="dp-settings__name-chip">{{type}}</span></td>
                <td>
                  <span class="dp-settings__name-chip" title="{{tokenEndpoint}}"
                    style="display:inline-block;max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">{{tokenEndpoint}}</span>
                </td>
                <td>
                  {{#if enabled}}
                  <span class="dp-settings__status-badge dp-settings__status-published">
                    <span class="dp-settings__status-dot"></span>Enabled
                  </span>
                  {{else}}
                  <span class="dp-settings__status-badge dp-settings__status-draft">
                    <span class="dp-settings__status-dot"></span>Disabled
                  </span>
                  {{/if}}
                </td>
                <td>
                  <div class="dp-settings__row-actions">
                    <button class="dp-settings__icon-btn dp-settings__km-edit-btn" type="button" data-id="{{id}}" title="Edit">
                      <i class="bi bi-pencil"></i>
                    </button>
                    <button class="dp-settings__icon-btn dp-settings__icon-btn--danger dp-settings__km-delete-btn" type="button"
                      data-id="{{id}}" data-name="{{displayName}}" title="Delete">
                      <i class="bi bi-trash"></i>
                    </button>
                  </div>
                </td>
              </tr>
              {{/keyManagers}}
              {{#unless keyManagers.length}}
              <tr><td colspan="5" class="dp-settings__tbl-empty">No key managers yet. Add one to get started.</td></tr>
              {{/unless}}
            </tbody>
          </table>
        </div>
      </div><!-- /#dp-settings__keymanagers -->
