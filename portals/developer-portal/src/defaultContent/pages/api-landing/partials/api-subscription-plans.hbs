{{#if subscriptionPlans.length}}
<section id="subscriptionPlans" class="dp-api-overview__plans-wrap" data-org-id="{{@root.orgId}}">

  <h2 class="dp-api-overview__plans-title">Subscription plans</h2>

  {{#each subscriptionPlans}}
  <div class="dp-api-overview__plan-card dp-subscription__card{{#if (isCurrentPlan id @root.subscriptions)}} dp-api-overview__plan-card--subscribed{{/if}}"
    id="subscriptionCard-{{id}}"
    data-policy-name="{{id}}">
    <div class="dp-api-overview__plan-ribbon" aria-hidden="true">
      <div class="dp-api-overview__plan-ribbon-label dp-api__ribbon-label">SUBSCRIBED</div>
    </div>
    <div class="dp-api-overview__plan-card-head">
      <div class="dp-api-overview__plan-name-row">
        <span class="dp-api-overview__plan-icon" style="background:#eef1f5;color:#3a4f6a;">
          <i class="bi bi-lightning-charge-fill"></i>
        </span>
        <span class="dp-api-overview__plan-name">{{#if displayName}}{{displayName}}{{else}}{{id}}{{/if}}</span>
      </div>
      {{#unless (isCurrentPlan id @root.subscriptions)}}
      <span class="dp-api-overview__recommended-badge" style="display:none;">RECOMMENDED</span>
      {{/unless}}
    </div>
    <div class="dp-api-overview__plan-limit">
      {{#if limits.length}}{{#each limits}}
      <span class="dp-api-overview__plan-count">{{#if (eq limitCount -1)}}∞{{else}}{{limitCount}}{{/if}}</span>
      <span class="dp-api-overview__plan-unit">{{limitTypeLabel limitType}}{{#if timeUnit}} / {{#unless (eq timeAmount 1)}}{{timeAmount}} {{/unless}}{{timeUnitLabel timeUnit}}{{/if}}</span>
      {{/each}}{{else if requestCount}}
      <span class="dp-api-overview__plan-count">{{requestCount}}</span>
      <span class="dp-api-overview__plan-unit">requests / min</span>
      {{else}}
      <span class="dp-api-overview__plan-count">—</span>
      {{/if}}
    </div>
    <div class="position-relative">
      {{#if ../isAuthenticated}}
        {{#if (isCurrentPlan id @root.subscriptions)}}
        <button type="button"
          class="dp-btn dp-btn--outline dp-btn--block dp-mcp__view-sub-btn dp-api__view-sub-btn"
          data-org-id="{{@root.orgId}}"
          data-api-id="{{../apiMetadata.id}}"
          data-policy-name="{{id}}"
          data-display-name="{{displayName}}">
          <i class="bi bi-eye"></i> View subscription
        </button>
        {{else}}
        <button id="dp-api__subscribe-btn-{{id}}" type="button"
          class="dp-btn dp-btn--outline dp-btn--block dp-api__subscribe-btn {{#if @root.isReadOnlyMode}}disabled{{/if}}"
          data-org-id="{{@root.orgId}}"
          data-api-id="{{../apiMetadata.id}}"
          data-plan-id="{{id}}"
          data-plan-name="{{id}}"
          data-display-name="{{displayName}}"
          onclick="handlePlanSubscription(this)"
          {{#if @root.isReadOnlyMode}}disabled{{/if}}><span class="dp-api-overview__plan-spinner"></span>Subscribe</button>
        {{/if}}
      {{else}}
      <a class="dp-btn dp-btn--outline dp-btn--block" href="{{@root.baseUrl}}/login">Subscribe</a>
      {{/if}}
    </div>
    <div class="dp-api-overview__plan-error">
      <i class="bi bi-exclamation-circle-fill" style="color:#c62828;font-size:15px;flex:none;margin-top:1px;"></i>
      <div>
        <div class="dp-api-overview__plan-error-title">Subscription failed</div>
        <div class="dp-api-overview__plan-error-desc">We couldn't subscribe you to this plan. Please try again.</div>
      </div>
    </div>
  </div>
  {{/each}}

  <script id="existing-subs-data" type="application/json">{{{json subscriptions}}}</script>
  <script src='/technical-scripts/api-subscription-plans.js' defer></script>
</section>

<!-- Subscription created token modal -->
<div id="apiTokenModal" class="dp-mcp__overlay" style="display:none;" role="dialog" aria-modal="true">
  <div class="dp-mcp__token-modal">
    <button id="apiTokenModalClose" class="dp-mcp__modal-close" aria-label="Close">&times;</button>
    <div class="dp-mcp__token-modal-header">
      <div class="dp-mcp__token-success-icon">
        <i class="bi bi-check-lg"></i>
      </div>
      <h2 class="dp-mcp__token-modal-title">Subscription created</h2>
      <p class="dp-mcp__token-modal-sub">Your subscription to the <strong id="apiTokenPlanName"></strong> plan is now active.</p>
    </div>
    <div class="dp-mcp__token-modal-body">
      <div class="dp-mcp__token-label">
        <i class="bi bi-key" style="color:#637282;"></i>
        <span>Subscription token</span>
      </div>
      <div class="dp-mcp__token-field dp-mcp__token-field--pink">
        <div id="apiTokenValue" class="dp-mcp__token-val dp-mcp__token-val--pink"></div>
        <button id="apiTokenCopyBtn" class="dp-copy__btn dp-mcp__token-copy-btn" title="Copy token">
          <span class="dp-copy__btn-icon"><i class="bi bi-copy"></i></span>
          <span class="dp-copy__btn-check"><i class="bi bi-check"></i> Copied</span>
        </button>
      </div>
      <div class="dp-mcp__token-warning">
        <i class="bi bi-exclamation-triangle-fill" style="color:#b8860b;flex:none;margin-top:1px;"></i>
        <span>Use this token as the <code class="dp-mcp__inline-code">Subscription-Key</code> header when invoking the API.</span>
      </div>
      <div class="dp-mcp__token-modal-footer">
        <button id="apiTokenModalDone" class="dp-btn dp-btn--primary">Done</button>
      </div>
    </div>
  </div>
</div>

<!-- Manage subscription modal -->
<div id="apiManageModal" class="dp-mcp__overlay" style="display:none;" role="dialog" aria-modal="true">
  <div class="dp-mcp__manage-modal">
    <div class="dp-mcp__manage-header">
      <div class="dp-mcp__manage-header-info">
        <span class="dp-mcp__manage-plan-icon"><i class="bi bi-lightning-charge-fill"></i></span>
        <div>
          <h2 id="apiManagePlanName" class="dp-mcp__manage-plan-name"></h2>
          <div class="dp-mcp__manage-subtitle">API subscription</div>
        </div>
      </div>
      <button id="apiManageClose" class="dp-mcp__modal-close" aria-label="Close">&times;</button>
    </div>
    <div class="dp-mcp__manage-body">
      <div class="dp-mcp__manage-status-row">
        <span class="dp-mcp__manage-status-label">Status</span>
        <span id="apiManageStatusBadge" class="dp-mcp__manage-status-badge dp-mcp__manage-status-badge--active">Active</span>
      </div>
      <div class="dp-mcp__token-label" style="margin-top:1.25rem;">
        <i class="bi bi-key" style="color:#637282;"></i>
        <span>Subscription token</span>
      </div>
      <div class="dp-mcp__token-field dp-mcp__token-field--manage">
        <div id="apiManageTokenVal" class="dp-mcp__token-val dp-mcp__token-val--manage"></div>
        <div class="dp-mcp__token-field-actions">
          <button id="apiManageRevealBtn" class="dp-mcp__token-action-btn" title="Reveal token">
            <i id="apiManageRevealIcon" class="bi bi-eye"></i>
          </button>
          <button id="apiManageCopyBtn" class="dp-copy__btn dp-mcp__token-copy-btn" title="Copy token">
            <span class="dp-copy__btn-icon"><i class="bi bi-copy"></i></span>
            <span class="dp-copy__btn-check"><i class="bi bi-check"></i> Copied</span>
          </button>
        </div>
      </div>
      <p class="dp-mcp__config-note" style="margin:0.625rem 0 0;">Use this token as the <code class="dp-mcp__inline-code">Subscription-Key</code> header when calling this API.</p>
      <hr class="dp-mcp__manage-divider">
      <div class="dp-mcp__manage-actions">
        <button id="apiManageSuspendBtn" class="dp-btn dp-mcp__manage-action-btn dp-mcp__manage-action-btn--suspend">
          <i class="bi bi-pause-circle"></i>
          <span class="dp-api__suspend-label">Suspend</span>
        </button>
        <button id="apiManageUnsubBtn" class="dp-btn dp-mcp__manage-action-btn dp-mcp__manage-action-btn--unsub">
          <i class="bi bi-trash"></i> Unsubscribe
        </button>
      </div>
    </div>
  </div>
</div>

<!-- Unsubscribe / plan-switch dialog -->
<div id="apiUnsubDialog" class="dp-mcp__overlay" style="display:none;" role="dialog" aria-modal="true">
  <div class="dp-mcp__unsub-dialog">
    <div class="dp-mcp__unsub-header">
      <span class="dp-mcp__unsub-icon"><i class="bi bi-exclamation-triangle-fill"></i></span>
      <h2 id="apiUnsubTitle" class="dp-mcp__unsub-title">Unsubscribe from plan?</h2>
    </div>
    <p id="apiUnsubDesc" class="dp-mcp__unsub-desc">Your applications will lose access to this API and any tokens will stop working. You can re-subscribe at any time.</p>
    <div class="dp-mcp__unsub-actions">
      <button id="apiUnsubKeepBtn" class="dp-btn dp-btn--outline">Cancel</button>
      <button id="apiUnsubConfirmBtn" class="dp-btn dp-mcp__unsub-confirm-btn">Unsubscribe</button>
    </div>
  </div>
</div>
{{/if}}
