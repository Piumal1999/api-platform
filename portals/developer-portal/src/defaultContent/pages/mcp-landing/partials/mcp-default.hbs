{{!-- MCP Server URL --}}
<div class="dp-api-overview__section">
  <div class="dp-api-overview__section-header">
    <h2 class="dp-api-overview__section-title">MCP Server URL</h2>
  </div>
  {{#if (eq resources.serverDetails "")}}
  <div class="dp-api-overview__endpoint-card">
    <span style="font-size:13px;color:#8a93a0;">No endpoints available</span>
  </div>
  {{else}}
  {{#unless (eq resources.serverDetails.productionURL "")}}
  <div class="dp-api-overview__endpoint-url-row" style="margin-top:0;">
    <i class="bi bi-link-45deg" style="color:#8a93a0;flex:none;"></i>
    <pre id="token_mcp_prod_url" class="dp-api-overview__endpoint-url dp-token__text">{{resources.serverDetails.productionURL}}</pre>
    <button type="button" class="dp-copy__btn" title="Copy URL" onclick="copyToken(this, 'mcp_prod_url')">
      <span class="dp-copy__btn-icon"><i class="bi bi-copy"></i></span>
      <span class="dp-copy__btn-check"><i class="bi bi-check"></i> Copied</span>
    </button>
  </div>
  {{/unless}}
  {{/if}}
</div>

{{!-- Tools --}}
{{#if schemaDefinition.tools.length}}
<div class="dp-api-overview__section">
  <div class="dp-api-overview__section-header">
    <h2 class="dp-api-overview__section-title">Tools</h2>
    <span class="dp-api-overview__section-count">{{schemaDefinition.tools.length}} tools</span>
  </div>
  {{#each schemaDefinition.tools}}
  <div class="dp-mcp__accordion">
    <div class="dp-mcp__accordion-head open" onclick="this.classList.toggle('open')">
      <span class="dp-mcp__accordion-head-label">
        <i class="bi bi-wrench"></i> {{this.name}}
      </span>
      <i class="bi bi-chevron-up dp-mcp__accordion-chevron"></i>
    </div>
    <div class="dp-mcp__accordion-body">
      {{#if this.description}}
      <p class="dp-mcp__accordion-desc">{{this.description}}</p>
      {{else}}
      <p class="dp-mcp__accordion-desc" style="color:#b0b8c4;">No description available.</p>
      {{/if}}
      {{#if this.inputSchema}}
      <div class="dp-mcp__json-block">
        <pre><code class="language-json">{{jsonBeautify this.inputSchema}}</code></pre>
      </div>
      {{/if}}
    </div>
  </div>
  {{/each}}
</div>
{{/if}}

{{!-- Resources --}}
{{#if schemaDefinition.resources.length}}
<div class="dp-api-overview__section">
  <div class="dp-api-overview__section-header">
    <h2 class="dp-api-overview__section-title">Resources</h2>
    <span class="dp-api-overview__section-count">{{schemaDefinition.resources.length}} resources</span>
  </div>
  {{#each schemaDefinition.resources}}
  <div class="dp-mcp__accordion">
    <div class="dp-mcp__accordion-head open" onclick="this.classList.toggle('open')">
      <span class="dp-mcp__accordion-head-label">
        <i class="bi bi-file-earmark-code"></i> {{this.name}}
      </span>
      <i class="bi bi-chevron-up dp-mcp__accordion-chevron"></i>
    </div>
    <div class="dp-mcp__accordion-body">
      {{#if this.description}}
      <p class="dp-mcp__accordion-desc">{{this.description}}</p>
      {{else}}
      <p class="dp-mcp__accordion-desc" style="color:#b0b8c4;">No description available.</p>
      {{/if}}
      {{#if this.uri}}
      <div class="dp-mcp__accordion-meta">
        <span class="dp-mcp__accordion-meta-key">URI</span>
        <code class="dp-mcp__accordion-meta-val">{{this.uri}}</code>
      </div>
      {{/if}}
      {{#if this.mimeType}}
      <div class="dp-mcp__accordion-meta">
        <span class="dp-mcp__accordion-meta-key">MIME Type</span>
        <code class="dp-mcp__accordion-meta-val">{{this.mimeType}}</code>
      </div>
      {{/if}}
    </div>
  </div>
  {{/each}}
</div>
{{/if}}

{{!-- Prompts --}}
{{#if schemaDefinition.prompts.length}}
<div class="dp-api-overview__section">
  <div class="dp-api-overview__section-header">
    <h2 class="dp-api-overview__section-title">Prompts</h2>
    <span class="dp-api-overview__section-count">{{schemaDefinition.prompts.length}} prompts</span>
  </div>
  {{#each schemaDefinition.prompts}}
  <div class="dp-mcp__accordion">
    <div class="dp-mcp__accordion-head open" onclick="this.classList.toggle('open')">
      <span class="dp-mcp__accordion-head-label">
        <i class="bi bi-chat-text"></i> {{this.name}}
      </span>
      <i class="bi bi-chevron-up dp-mcp__accordion-chevron"></i>
    </div>
    <div class="dp-mcp__accordion-body">
      {{#if this.description}}
      <p class="dp-mcp__accordion-desc">{{this.description}}</p>
      {{else}}
      <p class="dp-mcp__accordion-desc" style="color:#b0b8c4;">No description available.</p>
      {{/if}}
      {{#if this.arguments}}
      <div class="dp-mcp__json-block">
        <pre><code class="language-json">{{jsonBeautify this.arguments}}</code></pre>
      </div>
      {{/if}}
    </div>
  </div>
  {{/each}}
</div>
{{/if}}
