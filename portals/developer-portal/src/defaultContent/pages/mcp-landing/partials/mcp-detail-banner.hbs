<div class="dp-api-overview__hero">
  <div class="dp-api-overview__hero-top">
    <div class="dp-api-overview__hero-thumb">
      {{#if apiMetadata.apiImageMetadata.api-icon}}
      <img class="dp-api-overview__api-img" src="{{apiMetadata.apiImageMetadata.api-icon}}" alt="{{apiMetadata.name}}" />
      {{else}}
      <div class="dp-api-overview__api-avatar">{{firstTwoLetters (or apiMetadata.apiTitle apiMetadata.name)}}</div>
      {{/if}}
    </div>
    <div class="dp-api-overview__hero-title-row">
      <h1 class="dp-api-overview__hero-name">{{or apiMetadata.apiTitle apiMetadata.name}}</h1>
      <div class="dp-api-overview__hero-badges">
        {{#if apiMetadata.version}}
        <span class="dp-badge dp-badge--version">{{apiMetadata.version}}</span>
        {{/if}}
        <span class="dp-badge dp-badge--mcp"><i class="bi bi-cpu"></i> MCP</span>
        {{#if (eq apiMetadata.status "DEPRECATED")}}
        <span class="dp-badge dp-badge--deprecated"><i class="bi bi-exclamation-circle"></i> Deprecated</span>
        {{/if}}
      </div>
    </div>
  </div>
  <div class="dp-api-overview__hero-body">
    {{#if apiMetadata.description}}
    <p class="dp-api-overview__hero-desc">{{apiMetadata.description}}</p>
    {{/if}}
    {{#if apiMetadata.tags}}
    <div class="dp-tags">
      {{#apiMetadata.tags}}
      <span class="dp-tag dp-tag--white">{{this}}</span>
      {{/apiMetadata.tags}}
    </div>
    {{/if}}
    <div class="dp-api-overview__hero-actions">
      {{#if subscriptionPlans.length}}
        {{#unless subscriptions.length}}
      <a class="dp-btn dp-btn--primary" href="#subscriptionPlans">
        <i class="bi bi-bookmark-plus"></i> Subscribe
      </a>
        {{/unless}}
      {{/if}}
      <a class="dp-btn dp-btn--outline" href="{{baseUrl}}/mcp/{{apiMetadata.id}}/docs/specification">
        <i class="bi bi-book"></i> Documentation
      </a>
      {{#if showApiKeysNav}}
      <a class="dp-btn dp-btn--outline" href="{{baseUrl}}/mcp/{{apiMetadata.id}}/api-keys">
        <i class="bi bi-key"></i> API Keys
      </a>
      {{/if}}
    </div>
  </div>
</div>
