<div class="dp-mcp__config-panel">
  <h2 class="dp-api-overview__section-title" style="margin-bottom:0.875rem;">MCP Server Configuration</h2>
  <div class="dp-mcp__config-block">
    <button type="button" class="dp-copy__btn dp-mcp__config-copy" onclick="copyMcpConfig(this, '{{apiMetadata.id}}')" title="Copy configuration">
      <span class="dp-copy__btn-icon"><i class="bi bi-copy"></i></span>
      <span class="dp-copy__btn-check"><i class="bi bi-check"></i> Copied</span>
    </button>
    <pre id="mcp-cfg-{{apiMetadata.id}}" class="dp-mcp__config-pre"><code class="language-json">{
  "servers": {
    "{{apiMetadata.name}}": {
      "url": "{{resources.serverDetails.productionURL}}",
      "type": "http",
      "headers": {
        "Authorization": "Bearer ${token}"
      }
    }
  }
}</code></pre>
  </div>
  <p class="dp-mcp__config-note">Generate a token using a subscribed application and replace the placeholder.</p>

  {{#if subscriptionPlans.length}}
  <div style="margin-top:1.5rem;">
    {{> mcp-subscription-plans}}
  </div>
  {{/if}}
</div>
