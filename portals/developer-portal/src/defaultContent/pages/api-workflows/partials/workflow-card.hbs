<div class="dp-workflow-card__card" onclick="window.location.href='{{@root.baseUrl}}/api-workflows/{{this.id}}';">
      <div class="dp-workflow-card__card-head">
        <h3 class="dp-workflow-card__card-name">{{this.displayName}}</h3>
      </div>
      <div class="dp-workflow-card__card-body">
        <p class="dp-workflow-card__card-desc">{{this.description}}</p>
        {{#if this.sourcesPreview.length}}
        <div class="dp-workflow-card__powered-label">POWERED BY</div>
        <div class="dp-workflow-card__api-pills">
          {{#each this.sourcesPreview}}
          <span class="dp-workflow-card__api-pill">{{this.name}}</span>
          {{/each}}
          {{#if sourcesMoreCount}}
          <span class="dp-workflow-card__api-pill">+{{sourcesMoreCount}} more</span>
          {{/if}}
        </div>
        {{/if}}
      </div>
      <div class="dp-workflow-card__card-footer">
        {{#unless (eq this.agentVisibility "HIDDEN")}}
        <span class="dp-badge dp-badge--ai" title="AI agents can discover and use this workflow">
          <i class="bi bi-robot"></i> AI Ready
        </span>
        {{/unless}}
      </div>
    </div>
